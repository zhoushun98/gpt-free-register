[project]
name = "gpt-free-register"
version = "0.1.0"
description = "GPT 协议注册机"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl_cffi>=0.7.0",
    "flask>=3.0.0",
    "pycryptodome>=3.20.0",
    "pyotp>=2.9.0",
    "requests>=2.31.0",
]

[tool.uv]
package = false
